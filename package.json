{
  "name": "collectives",
  "version": "0.1.0",
  "devDependencies": {
    "grunt": "0.4.5",
    "http-server": "^0.8.5",
    "mocha": "^2.3.3",
    "node-sass": "^3.4.1",
    "nodemon": "^1.8.1"
  },
  "scripts": {
    "build-css": "node-sass scss/main.scss css/main.css",
    "watch": "nodemon -e scss -x \"npm run build-css\"",
    "http": "http-server",
    "all": "npm run watch & npm run http",
    "test": "mocha"
  }
}
